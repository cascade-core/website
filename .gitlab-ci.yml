stages:
        - deploy

deploy:
        stage: deploy
        only:
                - master@cascade/cascade-website
        tags:
                - no-git
        script:
                - 'curl -o - "$DEPLOY_URL"'

